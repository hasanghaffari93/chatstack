# Add your OpenAI API key here
OPENAI_API_KEY=your_openai_api_key

# Backend Configuration
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
JWT_SECRET=your_jwt_secret_here

# Environment Configuration
# Set to "production" for deployment, "development" for local development
# This affects:
# - Cookie security settings (secure=True in production)
# - Cookie SameSite settings ("none" for cross-domain in production, "lax" for same-domain in development)
# - CORS headers (includes "Cookie" in production for cross-domain requests)
# - ID token verification (strict in production, relaxed in development)
ENVIRONMENT=development

# For Production Deployment:
# ENVIRONMENT=production
# BASE_URL=https://your-backend-domain.com
# FRONTEND_URL=https://your-frontend-domain.com
# ALLOWED_ORIGINS=https://your-frontend-domain.com

BASE_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000
# MongoDB settings
MONGO_URI=mongodb://localhost:27017
MONGO_DB_NAME=chatstack 